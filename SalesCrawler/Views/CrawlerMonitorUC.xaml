<UserControl x:Class="SalesCrawler.Views.CrawlerMonitorUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SalesCrawler.Views"
             mc:Ignorable="d" 
             
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{StaticResource CrawlerVM}">
    <Grid>
        <DataGrid Margin="10,0,10,46" ItemsSource="{Binding Bots}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="StatusMessage" Binding="{Binding StatusMessage}" />
                <DataGridTextColumn Header="Elapsed Minutes" Binding="{Binding ElapsedMinutes}" />
            </DataGrid.Columns>
        </DataGrid>  
    </Grid>
</UserControl>
